# Smart Farm
# CLAUDE.md

> 农场盈亏决策驾驶舱 · 前端工作规范
> 本文件指导 Claude Code 在本仓库中的行为。每个会话开始时先通读本文件与 `tasks/lessons.md`。

---

## 1. 项目概述

我们在为**美国中小型农场**做一个利润规划工具,核心是一条**决策闭环**:把散落在 USDA、本地粮库、农机市场的数据,缝合成"今天这块地该不该卖、农机该不该买"的可执行决策。

- 当前阶段:MVP。**先只做玉米带 3 个州(IA / IL / IN)+ 玉米(corn)/ 大豆(soybean)**。
- 设计目标:小范围做扎实,但**架构必须可扩展**——后期加州、加作物、加数据源时,只改配置/数据,不动核心代码。
- 核心价值不是"再做一个计算器"(红海),而是**本地化的实时盈亏比对 + 卖买时机预警**。

---

## 2. 角色边界(最重要,优先级最高)

- **你(Claude Code)只负责前端**:Next.js + React + TypeScript。
- **后端由团队成员用 Dart 实现** —— 你**不写、不修改、不重构任何后端 / Dart 代码**。
- 前后端通过 **REST/JSON API** 通信。你始终针对**约定好的 API 契约**开发。
- 如果某个 API 契约还不存在:
  1. 不要擅自实现后端逻辑来"补上"。
  2. 在 `tasks/api-contracts.md` 里写出你**期望的**请求/响应结构(字段、类型、含义),标注 `TODO: 待后端确认`。
  3. 用 mock 数据 / MSW 推进前端开发,把契约假设集中在一处,便于后端对齐。
- **核心财务计算(保本价、盈亏)的权威来源在后端(Dart)**,保证 single source of truth。前端可做"录入时即时预览"的轻量估算,但**展示给用户的最终数字以后端返回为准**,前端不得把财务公式当作权威实现。

---

## 3. 架构(四层决策闭环)

| 层 | 职责 | 前端负责什么 |
|---|---|---|
| 市场数据层 | 期货(ZC/ZS)+ 本地现金价 / basis | 仅展示与轮询刷新,数据来自后端 |
| 农场档案层 | 农户成本结构、单产(APH)、农机 | **录入 UX 的主战场**,表单、校验、本地草稿 |
| 保本引擎 | 本地现金价 vs 真实保本价 | 调后端接口,展示结果与盈亏可视化 |
| 决策驾驶舱 | 卖出信号 · basis 预警 · 轮作建议 | 仪表盘、预警卡片、交互 |

> 农机在数据模型里是**成本变量**(影响每英亩保本价 / 回本年限),不是独立的估值模块。不要把它做成"和 TractorHouse 拼估值"的功能。

---

## 4. MVP 范围与可扩展性原则

- **作物、地区、数据源全部 data-driven**:用配置 / 枚举 / 数据库驱动,严禁硬编码 `if (state === 'IA')` 这类分支。
- 加一个新州 = 加一条配置 + 接一个数据源映射,**不应触碰组件或核心逻辑**。
- 作物相关参数(单位 bu/acre、保本公式系数、轮作比阈值)抽象成配置对象,玉米/大豆只是其中两个实例。
- 目录与类型按"层"组织,保持各层解耦,便于后期独立扩展。

---

## 5. 领域约束(必须遵守的业务规则)

这些是非技术人员容易踩坑、但决定产品对错的规则:

1. **"实时" = 当天粮库挂牌价,不是 tick 级行情。** 现金价按天/小时更新即可,不要按秒轮询,不要给用户"股票盘口"式预期。
2. **`basis = cash − futures`**,按 ZIP / 地区存成时间序列。这是盈亏算得准不准的命门。
3. **保本价 = 每英亩总成本 ÷ 单产**,并且**必须用农户的本地现金价比对**。
   - **禁止用期货价直接算农户盈亏** —— 那是错的。
4. **成本录入体验是护城河**:低摩擦、可沉淀、可复用上一季数据。任何涉及成本录入的改动,优先考虑"农户少填几个字段就能算出结果"。
5. **农机数据**:MVP 阶段走"农户手动输入目标机型 + 后端给参考价区间",**不要爬 TractorHouse / Sandhills**(法律与反爬风险)。

---

## 6. 前端技术约定

- Next.js(App Router)+ TypeScript(`strict: true`),禁用 `any`,除非有注释说明且不可避免。
- 所有跨前后端的数据结构都有显式类型,**类型定义与 `tasks/api-contracts.md` 同源**,改一处对齐另一处。
- 数据获取统一封装(如 `lib/api/`),不在组件里散落裸 `fetch`。
- 组件保持纯展示 / 容器分层;业务状态集中管理,避免 prop 透传地狱。
- 金额、单位、作物等领域概念用类型/常量收敛,不要在 JSX 里写魔法字符串。

---

# 工作流编排

## 1. 计划节点默认设置
- 对于任何非平凡任务(3 个以上步骤或架构决策),进入计划模式。
- 如果出现问题,立即停止并重新规划——不要继续推进。
- 在验证步骤中使用计划模式,而不仅仅是构建。
- 提前编写详细规格说明以减少歧义。

## 2. 子代理策略
- 大量使用子代理以保持主上下文窗口整洁。
- 将研究、探索和并行分析任务委托给子代理。
- 对于复杂问题,通过子代理投入更多计算资源。
- 每个子代理专注于一个任务以执行。

## 3. 自我提升循环
- 在用户进行任何修正后:用该模式更新 `tasks/lessons.md` 文件。
- 为自己制定规则,防止重复犯同样的错误。
- 无情地迭代这些教训,直到错误率下降。
- 在会话开始时回顾与项目相关的经验教训。

## 4. 完成前验证
- 不要在未证明其有效之前就标记任务完成。
- 在相关情况下,比较主分支与你所做更改之间的行为差异。
- 问问自己:"资深工程师会批准这个吗?"
- 运行测试,检查日志,演示正确性。

## 5. 追求优雅(平衡)
- 对于非小改动:暂停并问"有没有更优雅的方法?"
- 如果修复感觉很临时补丁:"知道我现在所掌握的一切,就实现优雅的解决方案。"
- 对于简单、显而易见的修复,跳过此步骤——不要过度工程化。
- 在展示自己的作品前先对其提出质疑。

## 6. 自主缺陷修复
- 当收到错误报告时:直接修复它,不要要求指导。
- 指出日志、错误和失败的测试——然后解决它们。
- 用户无需进行任何上下文切换。
- 在未被告知如何操作的情况下,自行修复失败的 CI 测试。

---

# 任务管理

1. **先计划**:将计划写入 `tasks/todo.md`,并包含可检查项。
2. **验证计划**:在开始实现前检查。
3. **跟踪进度**:在进行中标记任务完成状态。
4. **解释变更**:每一步提供高层级总结。
5. **记录结果**:向 `tasks/todo.md` 添加审查部分。
6. **记录经验教训**:修改后更新 `tasks/lessons.md` 文件。

---

# 核心原则

- **简洁优先**:让每次更改尽可能简单,影响最小化代码。
- **拒绝偷懒**:找出根本原因,不要使用临时修复,遵循高级开发者的标准。
- **最小影响**:更改应仅触及必要部分,避免引入错误。
