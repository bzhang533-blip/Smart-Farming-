# Smart Farm — Frontend (CLAUDE.md)

> 农场盈亏计算工具 · 前端工作规范(Next.js + React + TypeScript)
> 每个会话开始先读:本文件、`tasks/lessons.md`、`@docs/v1-alignment.md`(v1 契约与计算口径)。

@docs/v1-alignment.md

---

## 0. 当前阶段:v1(IMPORTANT — 先读这里)

v1 = 一个**最简单的单作物盈亏计算器**:农户录入 → 算出每英亩 margin、保本价、价格×单产敏感性 → 存 / 读场景。先把这一片做扎实,其他以后再说。

**v1 IN**
- 作物:corn / soybean(预留一个 other 槽)。
- 录入:单产(APH 或预期)、**现金价(农户手动填)**、各项成本(带地区默认值,可改)、土地 / 农机成本(各一个可填的 `$/acre` 数字)。
- 输出:每英亩损益卡、保本价 / 保本单产、净 margin;整场美元汇总;**交互式价格×单产敏感性热力图**;存 / 读场景。

**v1 OUT(不要建)**
- 行情 / 期货 / basis 数据源、卖买信号、任何预警、决策驾驶舱、轮作建议 —— v1 一律不碰。**现金价是手填输入,v1 不接任何实时数据源。**
- 营销 log、保险、A3-29 农机折旧引擎、PDF 报告、化肥 / 设备 / 设施等深度子计算器。
- 农机与土地成本在 v1 就是**一个数字**,不是计算模块,也不做估值。

---

## 1. 项目概述

为**美国中小型农场(玉米带,corn / soybean)**做利润规划工具。v1 的目标很直接:做一个**比现有那些笨重表格更好上手的盈亏 / 保本计算器** —— 农户填几个数,就知道作物得卖到多少钱才盈利。

架构保持**可扩展**(以后会加更多作物、地区、功能),但 v1 只做 §0 那一片,**不提前为未来功能铺摊子**。

---

## 2. 角色边界(优先级最高)

- **你(Claude Code)只负责前端**:Next.js + React + TypeScript。
- **后端由团队成员用 Dart 实现** —— 你**不写、不修改、不重构任何后端 / Dart 代码**。
- 前后端通过 **REST/JSON** 通信,始终针对**约定好的契约**开发。契约与 Scenario schema 见 `@docs/v1-alignment.md` 与 `tasks/api-contracts.md`(两者同源,改一处对齐另一处)。
- 契约不存在时:① 不擅自实现后端逻辑;② 在 `tasks/api-contracts.md` 写出**期望的**请求 / 响应结构,标 `TODO: 待后端确认`;③ 用 mock / MSW 推进前端。

### 计算归属(IMPORTANT)
- **财务计算引擎(margin、保本价、敏感性网格)在前端用 TS 实现,是唯一实现。** 写成纯函数,签名见 `@docs/v1-alignment.md` §7。
- **后端(Dart)不算账**,只提供 `GET /defaults` 默认值 + 场景持久化。前端不要指望后端返回算好的 margin。
- 理由:① 敏感性拖动条要实时重算、不走后端往返;② 计算只有一份实现,不让 TS / Dart 两份公式飘掉。

---

## 3. v1 数据流

```
农户录入 (Input layer)  →  计算引擎 (Calc engine, 纯 TS 函数)  →  输出 (P&L · 保本价 · 敏感性热力图)
        ↑                                                                    ↓
后端 GET /defaults(默认值)                                        后端 存 / 读场景
```

- 前端拥有从录入到输出的全部逻辑;后端只管默认值与持久化。
- 计算口径(revenue/acre、totalExpense、breakeven、sensitivity grid)与 Scenario schema 以 `@docs/v1-alignment.md` 为准。
- 农机在数据模型里是**成本变量**(影响每英亩保本价),不是独立估值模块,不要做成「和 TractorHouse 拼估值」。
- 代码按模块解耦,方便以后加东西 —— 但现在不预建未来的功能。

---

## 4. 可扩展性原则

- **作物、地区、成本项尽量 data-driven**:用配置 / 枚举驱动,别写 `if (state === 'IA')` 这类硬编码分支。
- 加一个新州 / 新作物,理想情况是加一条配置,不碰核心逻辑。
- 作物参数(单位 bu/acre、保本公式系数)抽象成配置对象,corn / soybean 只是其中两个实例。

---

## 5. 领域约束(必须遵守)

1. **保本价 = 每英亩总成本 ÷ 单产**,并且**必须用农户填的本地现金价来比对盈亏**。**禁止用期货价算农户盈亏 —— 那是错的。**
2. **成本录入体验是护城河**:低摩擦、可沉淀、可复用上一季数据。任何涉及成本录入的改动,优先考虑「农户少填几个字段就能算出结果」。
3. **农机数据**:农户手动输入一个 `$/acre` 成本(或目标机型 + 后端参考价区间),**不要爬 TractorHouse / Sandhills**(法律与反爬风险)。

---

## 6. 前端技术约定

- Next.js(App Router)+ TypeScript(`strict: true`),禁用 `any`,除非有注释说明且不可避免。
- 所有跨前后端的数据结构都有显式类型,**类型定义与 `tasks/api-contracts.md` / `docs/v1-alignment.md` 同源**,改一处对齐另一处。
- 数据获取统一封装(如 `lib/api/`),不在组件里散落裸 `fetch`。
- 组件保持纯展示 / 容器分层;业务状态集中管理(轻量 store 即可,如 Zustand),避免 prop 透传地狱。
- 金额、单位、作物等领域概念用类型 / 常量收敛,不要在 JSX 里写魔法字符串。

---

## 7. 命令(Claude Code 直接用,不要猜)

> 全部在 `frontend/` 目录下运行(npm,无 pnpm/yarn lockfile)。

- 安装:`npm install`
- 开发:`npm run dev`(Next dev,→ http://localhost:3000)
- 构建:`npm run build`(production build,**含类型检查**)
- 类型检查:`npx tsc --noEmit`
- Lint:`npm run lint`(= `eslint`)
- 测试:**暂未配置**(`package.json` 无 `test` 脚本)。需要时再引入 vitest,先在 `tasks/todo.md` 立计划。

---

# 工作流编排

## 1. 计划节点默认设置
- 任何非平凡任务(3 个以上步骤或架构决策)进入计划模式。
- 出问题立即停止并重新规划,不要硬推进。
- 验证步骤也用计划模式,不只是构建阶段。
- 提前写详细规格以减少歧义。

## 2. 子代理策略
- 大量使用子代理,保持主上下文窗口整洁。
- 把研究、探索、并行分析委托给子代理。
- 复杂问题通过子代理投入更多算力。
- 每个子代理只专注一个任务。

## 3. 自我提升循环
- 用户做任何修正后,用该模式更新 `tasks/lessons.md`。
- 给自己定规则,防止重复犯同样的错。
- 无情迭代这些教训,直到错误率下降。
- 会话开始时回顾与当前任务相关的经验教训。

## 4. 完成前验证
- 未证明有效之前不要标记任务完成。
- 相关时,比较主分支与你改动之间的行为差异。
- 自问:「资深工程师会批准这个吗?」
- 跑测试、查日志、演示正确性。

## 5. 追求优雅(平衡)
- 非小改动:暂停并问「有没有更优雅的方法?」
- 修复像临时补丁时:「以现在掌握的一切,实现优雅的方案。」
- 简单显而易见的修复跳过此步,不要过度工程化。
- 展示前先质疑自己的作品。

## 6. 自主缺陷修复
- 收到错误报告时直接修,不要等指导。
- 指出日志、错误、失败的测试,然后解决它们。
- 不让用户做上下文切换。
- 没被告知怎么做时,自行修复失败的 CI 测试。

---

# 任务管理

1. **先计划**:把计划写入 `tasks/todo.md`,含可勾选项。
2. **验证计划**:开始实现前检查。
3. **跟踪进度**:进行中标记完成状态。
4. **解释变更**:每步给高层级总结。
5. **记录结果**:向 `tasks/todo.md` 添加 review 部分。
6. **记录教训**:修改后更新 `tasks/lessons.md`。

---

# 核心原则

- **简洁优先**:每次更改尽可能简单,代码影响最小化。
- **拒绝偷懒**:找根本原因,不用临时修复,遵循高级开发者标准。
- **最小影响**:只触及必要部分,避免引入新错误。
